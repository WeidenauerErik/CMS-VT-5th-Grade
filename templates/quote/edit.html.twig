<!DOCTYPE html>

<title>Edit Quote</title>

{% block body %}
    <h1>Edit Quote</h1>

    {{ include('quote/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_quote_index') }}">back to list</a>

    {{ include('quote/_delete_form.html.twig') }}
{% endblock %}
